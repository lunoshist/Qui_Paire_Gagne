# Bases serveur (client Vite). Défauts = wrangler dev en local (port 8787).
# Créer une salle : POST {VITE_API_URL}/api/rooms
# Rejoindre       : WebSocket {VITE_WS_URL}/api/ws?room=CODE
VITE_API_URL=http://localhost:8787
VITE_WS_URL=ws://localhost:8787
