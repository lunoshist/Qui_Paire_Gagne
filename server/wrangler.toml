name = "qui-paire-gagne-server"
main = "src/index.ts"
compatibility_date = "2025-06-01"

# Une room = un Durable Object autoritatif (lobby + partie), WebSocket Hibernation.
[[durable_objects.bindings]]
name = "GAME_ROOM"
class_name = "GameRoom"

# Backend SQLite obligatoire pour le free tier des Durable Objects (voir D-005 / architecture.md).
# Rien n'est déployé : remplacement propre de la classe SQLite du scaffold (EchoRoom → GameRoom).
[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameRoom"]
